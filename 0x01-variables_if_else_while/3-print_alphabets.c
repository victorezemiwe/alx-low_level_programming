#include <stdio.h>

/**
 * main - prints alpha in lower and upper case
 * Return: Always 0
 */
int main(void)
{
char fe;
for (fe = 'a'; fe <= 'z'; fe++)
putchar(fe);
for (fe = 'A'; fe <= 'z'; fe++)
putchar(fe)
putchar('\n');
return (0);
}
